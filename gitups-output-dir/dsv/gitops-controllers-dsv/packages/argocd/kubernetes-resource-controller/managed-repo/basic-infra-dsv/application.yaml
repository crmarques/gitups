apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: basic-infra-dsv
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://example.invalid/gitops/basic-infra-dsv.git"
    path: "."
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
