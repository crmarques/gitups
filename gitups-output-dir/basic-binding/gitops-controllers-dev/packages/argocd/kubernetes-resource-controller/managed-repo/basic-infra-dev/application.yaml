apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: basic-infra-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://example.invalid/gitops/basic-infra-dev.git"
    path: "."
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
