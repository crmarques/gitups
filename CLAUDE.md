# CLAUDE.md

Read [AGENTS.md](./AGENTS.md) before every request in this repository.

`AGENTS.md` is authoritative for invariants, workflow, reference-file protocol,
and the `agents/plans/` rule. Do not read `agents/plans/*` unless the user
explicitly cites a path in the current request.

If `AGENTS.md` is missing or empty, stop and ask the user.
