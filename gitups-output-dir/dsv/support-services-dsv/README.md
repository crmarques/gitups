# support-services-dsv

Repo rendered by gitups from FullProvision "dsv".

## Packages

- **gitea-config** (local/gitea, renderer=raw, role=workload) → `packages/gitea/config`
- **keycloak-config** (local/keycloak, renderer=raw, role=workload) → `packages/keycloak/config`
- **service-mesh-config** (local/service-mesh, renderer=raw, role=workload) → `packages/service-mesh/config`
- **vault-config** (local/vault, renderer=raw, role=workload) → `packages/vault/config`
